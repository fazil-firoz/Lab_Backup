//add n numbers

#include <stdio.h>

int main ()
{
	int i,sum,n,a[50];
	printf("Enter the number of elements in array : ");
	scanf("%d",&n);
	printf("Enter the numbers : ");

	for (int i=0;i<=n;i++)
	{
		scanf("%d",&a[i])
	}

	printf("%d",a)
	
}
